  <div class="form-group">
                {{ form_row(form.nom)}}
            </div>
            <div class="form-group">
                {{ form_row(form.prix)}}
            </div>
            <div class="form-group">
                {{ form_row(form.description)}}
            </div>
            <div class="form-group">
                {{ form_row(form.createdAt)}}
            </div>
              <div class="form-group">
                {{ form_row(form.tissus)}}
            </div>